# AES 详情页 Template

> **归属：AES Product Design。** 本 Reference 是 AES（深信服下一代端点安全）产线专属 Template，不作为 Common Design 的通用模板。
> `Coverage: override`

## 目录

- 使用方式
- 容器选型：抽屉详情与下钻详情
- 通用页面结构
- 顶部标题区与对象概要区
- 正文组织：分区与 Tab
- 抽屉详情参考基准
- 下钻详情参考基准
- 页面类型、templateId 与封装状态
- 实现绑定
- 视觉与业务证据
- 状态、异常与边界
- 示例

## 1. 使用方式

本 Reference 用于 AES PC 端对象详情的页面选型、信息组织和交互设计。Theme 或用户已锁定详情容器、正文组织或连续浏览能力时直接执行，不得重新选型；只有未锁定事项才按本文判断。病毒、日志、任务、事件、客户端等业务名称不是新的页面类型。

- 先分别确定 `detail_container`、`entry_mode` 和 `content_organization`；前端已有编号不得反向限制详情结构。
- 病毒、日志、任务、事件、客户端等业务名称不是容器或入口类型。
- 用户已明确容器、宽度或交互时，以用户要求为准；若与硬边界冲突，必须指出冲突和影响。
- Theme 已锁定容器或交互时，以锁定契约为准；若与硬边界冲突，返回 Theme，不得在本 Template 自行切换。

执行顺序固定为：先确定页面类型、详情容器、入口关系、正文组织和详情变体；再核对该类型的前端封装状态；随后按封装状态使用前端封装或视觉与业务参照；最后填写 `templateId` 和 `encapsulation`。视觉参照不得替代前端封装核验。

## 2. 容器选型

### 2.1 容器与入口关系

| `detail_container` | `entry_mode` | 核心定位 | 典型参考 |
| --- | --- | --- | --- |
| `drawer` | `contextual` | 保留父页上下文，快速查看或轻量处理 | 病毒详情、日志详情、任务详情抽屉 |
| `page` | `drilldown` | 从来源页面进入独立空间完成深度理解或处理 | 安全事件详情、任务中心下钻详情、客户端详情 |
| `page` | `stable` | 具有稳定入口、可直接访问的独立详情任务 | 按当前业务确定 |
| `modal` | `contextual` | 当前流程中短时查看少量只读信息 | 按当前业务确定 |
| `other` | 按业务确定 | 以上容器无法表达、且有明确业务依据的详情结构 | 按完整 Template 契约定义 |

### 2.2 按顺序判断

1. 菜单、页头 Tab 或其他稳定入口直接承载详情任务时，使用 `page + stable`。
2. 需要独立 URL、浏览器前进后退、新标签页打开、分享定位、刷新恢复或深度处理时，使用 `page + drilldown`。
3. 需要频繁对照父列表、连续查看多条记录、快速标记或轻量处置时，使用 `drawer + contextual`。
4. 只需在当前流程中短时查看少量只读信息，并且关闭后立即返回原流程时，可以使用 `modal + contextual`。
5. 以上结构均无法保留已确认的主要任务和区域关系时，使用 `other` 并完整说明容器、入口和退出方式。

- Tab 数量不是容器选型条件。抽屉可以包含 2 个以上 Tab；根据内容复杂度、任务深度、父页上下文依赖和路由需求选择容器。

### 2.3 能力边界

| 判断项 | `drawer + contextual` | `page + drilldown/stable` | `modal + contextual` |
| --- | --- | --- | --- |
| 上下文 | 保留父页 | 独立页面；有来源时返回后恢复来源状态 | 保留当前流程 |
| 正文组织 | 分区或多个 Tab | 分区或多个 Tab | 少量连续只读内容 |
| 操作复杂度 | 查看和少量轻操作 | 深度分析、复杂配置或高风险操作 | 查看或单一轻操作 |
| 路由能力 | 通常不提供独立 URL | 使用独立路由 | 不提供独立 URL |
| 退出方式 | 右上角关闭；连续浏览可有底部关闭 | Stable 按导航退出；Drilldown 返回来源 | 右上角关闭或单一关闭操作 |

- 禁止连续嵌套超过 2 层抽屉。
- 二级内容复杂或属于独立任务时，从一级抽屉跳转下钻页。
- 查看与编辑复杂度差异明显时，查看使用抽屉，编辑跳转下钻页。
- 同一列表、同类对象、同一层级必须使用一致的详情容器。

## 3. 通用页面结构

抽屉和下钻页共享以下内容结构：

```text
详情内容
├── 容器导航区
│   ├── 抽屉：业务对象类型 + 详情                         关闭 ×
│   └── 下钻：返回入口 / 面包屑
├── 顶部概要区（横向布局，整体一般不超过 2 行）
│   ├── 左侧对象区（弹性宽度）
│   │   ├── 第 1 行：对象主标题 + 身份/等级/状态标签
│   │   └── 第 2 行：概要字段 1 │ 概要字段 2 │ 概要字段 3
│   └── 右侧操作区（固定在右侧，仅一行）
│       └── 操作 1 + 操作 2 + 更多
├── 页面提示区（按需，由 page-notice Pattern 决定）
└── 正文内容区
    ├── 分区平铺
    └── 或 Tab 切换
```

- 顶部概要区是一个横向父容器：左侧对象区可伸缩，右侧操作区固定宽度。
- 页面存在授权、风险、影响范围或其他业务提示时，预留 `noticeRegion`；提示条的展示条件和内容读取 `../04-patterns/page-notice.md`。
- 对象主标题、标签、概要字段均在左侧；业务操作均在右侧，不得混排。
- 左侧对象区一般不超过 2 行：第一行识别对象，第二行完成关键判断。
- 不得增加第三行概要字段。放不下的字段进入正文。
- 右侧操作始终横向排列，不得因标题或字段过长换到概要字段下方。

## 4. 顶部标题区与对象概要区

### 4.1 抽屉容器标题

- 抽屉标题固定使用“业务对象类型 + 详情”，例如“病毒详情”“事件详情”“任务详情”“规则详情”。
- 业务对象类型表示对象类别，不是当前记录的实例名称。
- 禁止只写“详情”“查看详情”，也不得用当前记录名称代替容器标题。
- 当前对象实例名称放在对象概要区第一行。

### 4.2 对象标题和左侧信息

- 左侧对象区第一行必须以当前对象实例的主标识为视觉主体，例如病毒名称、事件名称、任务名称或规则名称。
- 主标题使用比普通概要字段更高的视觉层级。无存量基准时使用 `14px`、`font-weight: 600`；同模块已有统一标题规范时沿用已有规范。
- 状态、等级、类型标签与主标题横向排列，可以位于标题前方或后方，但不能替代或削弱主标题。
- 图标只用于辅助识别对象类型，不能代替文本标题；主标题过长时单行省略并通过 Tooltip 展示完整内容，不得挤压右侧操作区。
- 实例没有自然名称时，使用最稳定、最可识别的业务主键作为主标题，不得显示空标题。

### 4.3 概要字段

- 第二行只放用户进入详情后首先用于识别、判断或处置的关键字段。
- 字段横向排列，一般控制在 3 至 5 个；字段较短且容器充足时可以适当增加，但不得形成第三行。
- 字段按“身份/范围 → 状态/结果 → 关键时间”的判断顺序组织，而不是照接口字段顺序罗列。
- 长描述、原始日志、完整路径、备注历史和低频字段进入正文。
- 空值统一显示 `-`；零值必须显示 `0`，不得按空值处理。

### 4.4 右侧操作

- 操作区固定在概要区右侧，仅一行横向排列。
- 最多外显 2 个业务操作；超出部分收入“更多”菜单。
- “更多”不计入 2 个业务操作上限，使用省略号图标并提供 Tooltip。
- 外显顺序按当前任务频率和重要性排列；危险操作不得仅因重要而默认外显，禁用操作必须展示具体原因。
- 操作执行后更新当前详情，并同步父列表；不改变当前查询上下文。

### 4.5 概要区背景

概要区只有两种表现：`plain` 和 `emphasized`。背景由内容语义决定，不得作为任意视觉选项。

#### `plain`：标准概要，默认使用

- 用于“对象主标题 + 标签 + 横向关键字段 + 操作”的标准对象概要。
- 不设置独立底色和装饰性卡片，直接在详情内容背景上排布。
- 信息通过标题层级、字段间距和正文分隔建立层次。
- 参考病毒详情。

```text
┌──────────────────────────────────────────────────────────┐
│ [高危] [木马]  Trojan.Win32.Agent       [处置] [标记] […] │
│ 场景：文件检测 │ 首次发现：… │ 状态：待处置 │ 终端：PC-01 │
└──────────────────────────────────────────────────────────┘
```

#### `emphasized`：语义聚合，满足条件才使用

- 仅用于必须被整体理解的身份上下文或关系上下文。
- 身份上下文是图标、名称、状态、描述等共同表达一个不可拆分的当前对象身份，并且正文依赖该身份上下文理解。
- 关系上下文是源对象、行为、目标对象、资产或关联结果组成的关系链，例如“进程 → 行为 → 目标对象”。
- 整块使用统一的中性浅色信息背景、`2px` 圆角和平台标准内边距；新页面不使用渐变背景。
- 参考日志详情的关系上下文；任务详情现有蓝色渐变属于历史实现，新设计应改用中性浅色背景或退回 `plain`。

```text
┌──────────────────────────────────────────────────────────┐
│ 浅色语义背景                                             │
│ [源进程 explorer.exe]  →  [访问行为]  →  [目标文件 A]    │
│ 关联资产：PC-01                         关联事件：事件 01 │
└──────────────────────────────────────────────────────────┘
```

以下情况不能作为增加背景的理由：字段较多、希望页面更丰富、其他页面使用了底色、仅需区分概要和正文。

## 5. 正文组织形式与布局

先选择正文组织形式，再执行对应布局规则。`sections` 是默认形式；Tab 用于解决内容过多或任务相互独立，不用于装饰页面。

### 5.1 第一步：选择平铺或 Tab

“正文内容模块”是有独立业务标题和完整内容的一组信息，例如执行信息、关联对象、处理结果；字段行不是独立模块。

1. 只有 1 个正文内容模块时，直接使用 `sections` 平铺，禁止生成只有一个 Tab 的标签页。
2. 有 2 个及以上模块时进入组织形式判断，仍默认使用 `sections` 平铺。
3. 只有平铺后内容明显过多，或模块属于用户不会连续阅读的独立任务/数据视图时，才使用 `tabs`。

以下任一情况成立，可将 2 个及以上模块组织为 Tab：

- 平铺后需要长距离滚动，用户难以快速定位目标模块。
- 各模块是相互独立的任务、数据集或分析视角，用户通常一次只查看其中一个。
- 各模块本身内容较重，分别包含表格、时间线、图表、原始数据或独立操作。
- 同一数据需要在结构化详情、原始日志等互斥视图间切换。

模块较多、但内容短且需要连续阅读时仍使用平铺；不得因模块达到某个数量直接使用 Tab。Tab 数量也不得参与抽屉与下钻页的容器选型。

```text
正文内容模块数量
├── 1 个 → 平铺
└── 2 个及以上 → 默认平铺并判断
    ├── 内容可在合理滚动范围内连续阅读 → 平铺
    └── 内容过多，或模块为独立任务 / 独立视图 → Tab
```

### 5.2 第二步：执行平铺布局

- 按用户阅读顺序纵向排列模块，标题使用明确业务名称，不使用“其他信息”“更多信息”。
- 分区标题参考“任务中心 - 任务详情 - 任务信息”抽屉：`14px`、`font-weight: 600`、默认行高 `36px`；左侧显示 `2px × 12px` 蓝色竖线并垂直居中。
- 竖线使用平台主色，AES 基准为 `#1c6eff`；同模块已有统一 Token 时使用对应 Token。
- 分区之间禁止使用分割线，以前一分区内容和下一标题之间默认 `16px` 的垂直间距建立层次。
- 同类字段使用描述列表，集合数据使用表格，过程和关联关系使用对应组件。
- `content_type=table` 时，将 `table-management` 写入该模块的 `pattern_requirements`，并传递 `host_template=detail`、`table_role=embedded`、抽屉宽度或正文可用宽度；不得先调用 List Template，也不得由 Table Pattern 重新决定详情容器、分区或 Tab。
- 分区内容不得套装饰性卡片；背景、边框和阴影不能代替标题与间距。

### 5.3 第三步：执行 Tab 布局

- Tab 采用顶部线型导航并使用稳定、唯一的标识；具体组件、属性、事件和插槽由 Component 层或编码阶段核验。
- Tab 名称直接表达内容或任务，例如“结构化详情 / 原始日志”“任务信息 / 执行记录”；少量字段不能独立成为 Tab。
- 抽屉允许使用 2 个以上一级 Tab。Tab 溢出时保留 `showAllTabsPanel` 能力，通过组件的全部标签入口访问，不压缩文字、换行或缩小字号。
- Tab 内允许按 5.2 使用少量平铺分区，但不得继续嵌套主要 Tab。
- 切换 Tab 保留已加载数据和局部状态；连续切换到另一条记录时回到默认 Tab。下钻页需要直接定位时将选中 Tab 写入路由。

## 6. 抽屉详情参考基准

### 6.1 宽度

- 默认宽度为 `640px`，用于单列字段、基础查看和少量分区。
- 宽抽屉为 `960px`，用于横向概要字段较多，或正文包含表格、关系内容、图形或多个 Tab。
- 用户可以根据明确的信息内容量指定其他宽度，最大不得超过 `1200px`。
- 合理内容在 `1200px` 内仍无法承载时，重新判断为下钻详情，不继续加宽。
- 同模块、同类型抽屉宽度保持一致，不因单条数据长短动态改变。

### 6.2 连续浏览抽屉

用于从当前列表结果中连续查看多条记录，通常为非模态抽屉。

```text
父列表（保留可见和当前查询状态）     ┌──────────────────────────────┐
                                    │ 病毒详情                关闭 × │
                                    ├──────────────────────────────┤
                                    │ 对象标题 + 标签       横向操作 │
                                    │ 概要字段 1 │ 字段 2 │ 字段 3 │
                                    ├──────────────────────────────┤
                                    │ 分区正文，或多个 Tab          │
                                    │                              │
                                    ├──────────────────────────────┤
                                    │ [上一个] [下一个] [关闭]      │
                                    └──────────────────────────────┘
```

- `sequence_navigation: enabled`，底部固定显示“上一个 / 下一个 / 关闭”。
- 上一个、下一个以打开抽屉时列表的搜索、筛选、左树范围和排序结果为序列。
- 第一条禁用“上一个”，最后一条禁用“下一个”，仅一条时两者均禁用；按钮位置保持稳定。
- 切换期间禁用上一个和下一个，保留抽屉骨架或加载态，不先闪空。
- 快速连续点击时，仅最后一次请求可以更新详情，避免旧请求覆盖新记录。
- 切换成功后重置正文滚动位置、默认 Tab、展开项、局部选择和错误状态。
- 切换失败时保留原记录和原序号，并提示失败，不得显示半条新数据。
- 当前记录被删除后优先展示下一条；没有下一条时展示上一条；序列为空时关闭抽屉。
- 用户改变父列表搜索、筛选、左树、排序或分页时关闭抽屉，避免切换序列与列表不一致。
- 不同时打开多个详情抽屉进行对比；需要对比时使用下钻页的新标签页能力或专门的对比功能。

### 6.3 单记录抽屉

用于临时查看一条记录，不承担列表连续浏览，通常为模态抽屉。

```text
┌────────────────────────────────────────────┐
│ 任务详情                            关闭 × │
├────────────────────────────────────────────┤
│ 任务名称 + 类型标签                 状态/操作 │
│ 执行对象：… │ 创建时间：… │ 发起人：…       │
├────────────────────────────────────────────┤
│ 执行信息                                   │
│ 字段 …                                     │
│                                            │
│ 任务信息                                   │
│ 字段 …                                     │
└────────────────────────────────────────────┘
```

- `sequence_navigation: disabled`，不显示上一个和下一个。
- 只读详情通过右上角关闭，不生成空白底部操作栏。
- 存在必须完成的业务操作时才增加固定底部操作区；关闭不重复作为主业务按钮。
- 点击遮罩是否关闭遵循平台抽屉规范；存在编辑态或未保存输入时禁止遮罩关闭，并提供离开确认。

### 6.4 抽屉二次结构

抽屉只允许以下四种结构组合，用于选参考逻辑，不是四种页面类型：

| 连续浏览 | 正文组织 | 适用说明 | 参考 |
| --- | --- | --- | --- |
| 有 | 分区 | 快速逐条查看，内容属于同一判断任务 | 病毒详情 |
| 有 | Tab | 快速逐条查看，不同数据视图相互独立 | 日志详情 |
| 无 | 分区 | 单条轻量详情，按模块顺序阅读 | 任务详情抽屉 |
| 无 | Tab | 单条详情存在两个独立内容任务 | 按规则组合，优先复用同模块实现 |

## 7. 下钻详情参考基准

### 7.1 分区下钻页

```text
┌──────────────────────────────────────────────────────────┐
│ ‹ 返回 / 面包屑                                          │
├──────────────────────────────────────────────────────────┤
│ 对象主标题 + 标签                      [操作 1] [操作 2] […] │
│ 概要字段 1 │ 概要字段 2 │ 概要字段 3 │ 概要字段 4          │
├──────────────────────────────────────────────────────────┤
│ 模块一                                                    │
│ 内容                                                      │
├──────────────────────────────────────────────────────────┤
│ 模块二                                                    │
│ 内容                                                      │
└──────────────────────────────────────────────────────────┘
```

- `sequence_navigation: not-applicable`，不使用上一个、下一个或关闭。
- 使用返回入口和浏览器导航；返回后恢复父列表搜索、筛选、排序、分页和滚动位置。
- 模块属于同一深度处理任务且需要顺序理解时采用分区平铺。
- 顶部概要和关键操作在滚动后是否吸顶，沿用同模块既有页面；无存量基准时不额外吸顶。

### 7.2 Tab 下钻页

```text
┌──────────────────────────────────────────────────────────┐
│ ‹ 返回 / 面包屑                                          │
├──────────────────────────────────────────────────────────┤
│ 对象主标题 + 标签                      [操作 1] [操作 2] […] │
│ 概要字段 1 │ 概要字段 2 │ 概要字段 3 │ 概要字段 4          │
├──────────────────────────────────────────────────────────┤
│ [总览] [关联告警] [攻击分析] [处置记录]                   │
├──────────────────────────────────────────────────────────┤
│ 当前 Tab 的独立内容、操作和状态                           │
└──────────────────────────────────────────────────────────┘
```

- 多个独立任务、数据集或分析视角使用 Tab；是否采用下钻页仍由任务复杂度、上下文依赖和路由需求决定，与 Tab 数量无关。
- 默认 Tab 必须对应最高频的首要任务，不机械命名为“基本信息”。
- 需要直接定位时，将当前 Tab 写入路由；刷新和前进后退应恢复当前 Tab。
- Tab 切换不重复请求稳定数据；时效性数据按业务刷新策略更新。
- 页面级操作放在顶部右侧；只作用于当前 Tab 的操作放在 Tab 内容工具区，不能混淆作用范围。

### 7.3 下钻二次结构

| 正文组织 | 适用说明 | 参考 |
| --- | --- | --- |
| 分区 | 信息多但属于一个连续处理任务 | 按任务中心详情的连续模块逻辑组织 |
| Tab | 多个独立分析或管理任务 | 安全事件详情、客户端详情 |

## 8. 页面类型、templateId 与封装状态

完成容器、入口关系、正文组织、详情变体和视觉业务参照核对后再执行本节。`templateId` 和 `encapsulation` 是页面设计结果的模板契约，不参与前面的页面结构选型；即使本节在文档中位于视觉证据之前，也不得跳过前端封装状态核验。

| 页面类型 | 已确认结构 | `templateId` | `encapsulation` | 执行方式 |
| --- | --- | --- | --- | --- |
| 抽屉详情页 | Drawer + Contextual | `page-detail-drawer` | `true` | 优先复用抽屉详情骨架，未覆盖部分继续补充 |
| 下钻详情页 | Page + Drilldown | `page-detail-drilldown` | `false` | 按本 Template 契约实现 |
| 自定义页面类型 | 没有既有 AES Template 可承载的详情结构 | `custom` | `false` | 按完整 Template 契约实现 |

设计详情页时必须返回：

```yaml
template_contract:
  templateId: page-detail-drawer | page-detail-drilldown | custom
  encapsulation: true | false
  customReason: ""
  detail_container: page | drawer | modal | other
  entry_mode: stable | drilldown | contextual
  content_organization: sections | tabs
  sequence_navigation: enabled | disabled | not-applicable
  drawer_width: 640 | 960 | user-specified
  header_contract:
    drawer_title: 业务对象类型 + 详情 | not-applicable
    object_title: 当前对象实例的主标识
    summary_presentation: plain | emphasized
    presentation_reason: standard-summary | identity-context | relationship-context
    summary_fields: []
    exposed_actions: []
    more_actions: []
    noticeRegion: page-notice | none
    selection_reason: 说明容器、正文组织和概要表现的判断依据
  body_modules:
    - module_id: ""
      content_type: descriptions | table | timeline | chart | raw-data | other
      pattern_requirements: []
  reference_page: 真实参考页面
```

- `content_organization`、`sequence_navigation` 是结构能力，不得包装为新的页面类型。
- Drawer 必须返回 `drawer_width`；其他容器返回 `not-applicable`。
- 无论是否封装都沿用该页面模板的稳定编号；`encapsulation: true` 时，`templateId` 必须与前端页面封装编号一致；`encapsulation: false` 时按完整契约自行实现。
- 没有既有 AES Template 可承载时填写 `templateId: custom`、`encapsulation: false` 和 `customReason`。
- `encapsulation: false` 不阻断设计或开发，也不得为了获得前端封装改变已确认的详情结构。
- 本 Reference 对 AES 详情页的容器、概要区、正文组织、连续浏览和关闭方式采用 `override`。通用组件 API、导航适配和基础可访问性要求不得改变上述 AES 详情结构。

## 9. 实现绑定

- `encapsulation: true` 时在编码阶段核验目标分支中模板的真实入口和参数，封装未覆盖的详情能力按本 Template 契约补充实现。
- `encapsulation: false` 时不得声称复用了页面模板，按本 Template 契约和项目组件实现。

## 10. 视觉与业务证据

本节用于页面还原和视觉校验，不改变前面的页面类型、容器、入口、正文组织和详情变体判断。

- `encapsulation: true` 时，先核验并优先使用前端页面封装；本节只用于校验封装效果、补充封装未覆盖内容和还原 AES 业务差异。
- `encapsulation: false` 时，前端没有可直接调用的页面封装，本节作为页面结构和视觉还原参考，结合当前 Template 契约自行实现。
- 参考页面不自动成为当前详情的字段、操作、状态、权限或正文模块；这些内容仍由当前需求和上游设计契约决定。

以下路径均相对于前端工程 `/Users/sangfor/Documents/aes-mgr-front0830`。

### 抽屉详情

| 详情结构 | 参考页面 | 关键源码 |
| --- | --- | --- |
| 连续浏览抽屉 | 病毒详情 | `app/aes-virus/src/view/virus_list/components/virus_table.vue` |
| 病毒详情正文 | 病毒信息、资产信息、检测与处置记录 | `app/aes-virus/src/view/virus_detail/index.vue` |
| 单记录抽屉 | 任务详情 | `app/aes-task/src/view/components/drawer_task_detail.vue` |
| 行为详情抽屉 | 安全事件行为详情 | `app/aes-incident/src/view/mod_sec_event/components/behavior_detail_drawer/index.vue` |

病毒详情应同时核对列表中的抽屉容器和详情正文：前者负责宽度、连续浏览及关闭方式，后者负责概要与正文模块。

### 下钻详情

| 详情结构 | 参考页面 | 关键源码 |
| --- | --- | --- |
| 安全事件详情 | 事件分析下钻页 | `app/aes-incident/src/view/mod_sec_event/event_detail/index.vue` |
| 任务详情 | 任务中心下钻页 | `app/aes-task/src/view/task_detail/index.vue` |
| 客户端详情 | 终端详情下钻页 | `app/aes-agent/src/view/agent_detail/index.vue` |

参考实现时只复用与目标详情类型匹配的容器、概要区、正文组织和导航关系。现有页面中的业务字段、历史视觉样式和特定操作不能自动成为新详情页规则。

## 11. 状态、异常与边界

### 11.1 加载与失败

- 首次加载使用与页面结构匹配的骨架或加载态，避免空白容器。
- 概要与正文可以分开加载；概要失败时不得显示错误对象标题，正文局部失败只替换对应分区或 Tab。
- 整体失败显示重试入口，并保留退出或返回能力。
- 操作成功后刷新受影响区域并同步父列表；失败时保留当前内容和用户位置。

### 11.2 空值与长内容

- 普通空字段显示 `-`；整个模块没有数据时显示模块级空状态，不罗列整屏 `-`。
- 长标题、路径、哈希、ID 和描述必须提供省略、换行、复制或 Tooltip 中合适的一种完整查看方式。
- 原始 JSON、日志和代码类内容使用等宽字体，支持复制；数据量大时使用虚拟滚动或按需加载。
- 字段和值不得与操作区重叠，不得因国际化长文案改变固定操作位置。

### 11.3 权限与操作

- 无查看权限时不进入详情；局部无权限时在对应模块展示明确状态。
- 无操作权限的动作按平台规则隐藏或禁用；禁用时必须说明原因。
- 高风险操作必须二次确认，成功与失败均给出明确反馈。
- 编辑存在未保存内容时，关闭抽屉、返回、切换 Tab 或跳转前均按影响范围提供离开确认。

## 12. 完整示例

### 12.1 病毒列表进入病毒详情

```yaml
detail_container: drawer
entry_mode: contextual
content_organization: sections
sequence_navigation: enabled
drawer_width: 960
header_contract:
  drawer_title: 病毒详情
  object_title: Trojan.Win32.Agent
  summary_presentation: plain
  presentation_reason: standard-summary
  summary_fields: [检测场景, 首次发现时间, 末次发现时间, 处置状态]
  exposed_actions: [处置, 标记]
  more_actions: [其他低频操作]
selection_reason: 需要保留病毒列表上下文并连续查看多条记录；正文属于同一分析任务。
reference_page: 病毒列表 - 病毒详情
```

```text
父列表                               ┌────────────────────────────────────┐
                                     │ 病毒详情                    关闭 × │
                                     ├────────────────────────────────────┤
                                     │ [高危][木马] Trojan.Win32.Agent    │
                                     │                    [处置][标记][…] │
                                     │ 场景：文件 │ 首次：… │ 状态：待处置 │
                                     ├────────────────────────────────────┤
                                     │ 基础信息                           │
                                     │ 文件信息 …                         │
                                     │ 关联终端 …                         │
                                     ├────────────────────────────────────┤
                                     │ [上一个] [下一个] [关闭]           │
                                     └────────────────────────────────────┘
```

### 12.2 日志调查进入日志详情

```yaml
detail_container: drawer
entry_mode: contextual
content_organization: tabs
sequence_navigation: enabled
drawer_width: 960
header_contract:
  drawer_title: 日志详情
  object_title: 日志主标识或行为名称
  summary_presentation: emphasized
  presentation_reason: relationship-context
  summary_fields: [发生时间, 资产, 行为结果]
  exposed_actions: [关联调查]
  more_actions: []
selection_reason: 需在列表中连续调查；关系上下文必须整体理解；结构化详情和原始日志是独立视图。
reference_page: 日志调查 - 日志详情
```

```text
┌──────────────────────────────────────────────┐
│ 日志详情                              关闭 × │
├──────────────────────────────────────────────┤
│ 日志主标识                         [关联调查] │
│ ┌──────────────────────────────────────────┐ │
│ │ explorer.exe → 文件访问 → target.exe    │ │
│ │ 资产：PC-01              关联事件：事件A │ │
│ └──────────────────────────────────────────┘ │
├──────────────────────────────────────────────┤
│ [结构化详情] [原始日志]                      │
│ 当前 Tab 内容                                │
├──────────────────────────────────────────────┤
│ [上一个] [下一个] [关闭]                     │
└──────────────────────────────────────────────┘
```

### 12.3 单条任务详情抽屉

```yaml
detail_container: drawer
entry_mode: contextual
content_organization: sections
sequence_navigation: disabled
drawer_width: 640
header_contract:
  drawer_title: 任务详情
  object_title: Windows 终端快速扫描
  summary_presentation: plain
  presentation_reason: standard-summary
  summary_fields: [任务类型, 执行状态, 创建时间, 发起人]
  exposed_actions: []
  more_actions: []
selection_reason: 仅查看单条任务信息，无连续浏览和复杂处理；信息可按执行信息、任务信息顺序阅读。
reference_page: 任务中心 - 任务详情抽屉
```

### 12.4 安全事件下钻详情

```yaml
detail_container: page
entry_mode: drilldown
content_organization: tabs
sequence_navigation: not-applicable
drawer_width: not-applicable
header_contract:
  drawer_title: not-applicable
  object_title: 勒索软件攻击事件
  summary_presentation: plain
  presentation_reason: standard-summary
  summary_fields: [事件等级, 事件状态, 影响资产, 最近发生时间]
  exposed_actions: [处置, 关闭事件]
  more_actions: [导出, 其他低频操作]
selection_reason: 包含多个独立分析任务和多类数据，需要独立路由及沉浸式处理。
reference_page: 安全事件详情
```
